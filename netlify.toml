[build]
  command = "bun install --frozen-lockfile && bun run build"
  publish = ".next"

[build.environment]
  BUN_VERSION = "1.3.5"
  NODE_VERSION = "node"

[[plugins]]
  package = "@netlify/plugin-nextjs"
